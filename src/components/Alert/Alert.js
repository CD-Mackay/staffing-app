import React, { useRef } from "react";

import StyledAlert from "./StyledAlert";

// Component Imports

const Alert = () => {

  return (
    <StyledAlert>
   
    </StyledAlert>
  );
};

export default Alert;
